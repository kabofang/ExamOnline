#define MAX 500 //运算中的最高可能位数
#define NUMP 2 //已提供的素数个数
#define NUMG 50 //随机数g的位数
#define swap_byte(x,y) x^=y^=x^=y
void encon(int a,char* p,char* g,char *ans);
void encon(int a,char* p,char* g,char *ans);
void recon(int b,char *p,char *g,char *ans);
void getkey(int b,char *rec,char*p,char*g,char *ans);
void display(char*a);
extern char sa[MAX],p[MAX],g[MAX],au[MAX],sb[MAX];
extern int a;